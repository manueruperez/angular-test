// esta es una representacion claa de que debe tener cada objeto dentro del array
export interface Product {
    id: string;
    image: string;
    title: string;
    price: number;
    description: string;
}